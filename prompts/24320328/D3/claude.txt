StudyFlow 用户端前端 —— AI 对话记录导出
工具：Claude Code
日期：2026-09-03（D3）
阶段：接口与表单测试 + Git 提交 + 文档整理

========== 1 ==========
【提问】
用 curl 逐项验证 mock 接口是否符合契约：登录返回 token+user、重复邮箱 409、越权 404、打卡幂等、统计口径正确。

【收获】
发现并修复了一个真实 bug：`/plans/1/check-ins/date` 被「计划详情」分支先匹配，导致打卡接口返回「方法不支持」。给详情分支加「无第三段才走详情」判定后解决。

========== 2 ==========
【提问】
逐项测试表单校验：邮箱格式、密码必填、两次密码不一致、结束日期早于开始日期等场景是否被拦截。

【收获】
把「测试」落到可复现的步骤，形成 TC-01~03 的页面侧证据。

========== 3 ==========
【提问】
走查「登录→首页→新建计划→打卡→统计」完整流程，检查菜单高亮、跳转、加载/报错/空状态。

【收获】
从用户视角过一遍，确认交互完整，不只是「页面能渲染」。

========== 4 ==========
【提问】
用 git 初始化仓库，建 feat/wang-frontend 分支，写 .gitignore，提交语义化 commit。

【收获】
养成「功能分支开发 + 语义化提交信息」的协作习惯，不直接往 main 上推。

========== 5 ==========
【提问】
把本地分支推送到 GitHub 团队仓库新分支，不要直接推 main；遇认证问题帮我诊断。

【收获】
学会排查 Git 认证链路：没装 gh、没有 SSH key、没存凭据时会卡在 GCM 弹窗；改用 GCM 浏览器登录解决。

========== 6 ==========
【提问】
编写 README（启动方式、演示账号、mock 切换、目录归属、契约假设）。

【收获】
把「口头约定」沉淀成文档，标注了 3 条需要和后端核对的契约假设（注册返回 token、统计需 planId、打卡列表非分页）。
