# D3 日报

> 2026-09-03（第 3 天）

## 昨日遗留问题的回答
- 昨日自问「同一天重复打卡，会不会产生多条脏数据？」——今天用 curl 验证：对同一计划同一日期 PUT，返回的是更新后的同一条记录，条数不增，幂等成立。

## 今日目标
- 逐项验证接口契约和表单校验。
- git 提交并推送到团队仓库新分支。
- 补齐 README / todos / 日报 / 提示词记录。

## 实际进展
- 接口验证通过：登录返回 token + user、重复邮箱 409、越权访问他人计划 404、打卡幂等、统计口径正确（完成率 71.43%、连续 5 天）。
- 表单校验走查通过：邮箱格式、密码必填、两次密码一致、结束日期早于开始日期等均被拦截。
- git 初始化，建 `feat/wang-frontend` 分支并提交（fe10376），推送到团队仓库，未动 main。

## 遇到的问题与解决
- **打卡接口返回「方法不支持」**：排查发现 `/plans/1/check-ins/date` 被前面的「计划详情」分支先匹配了。给计划详情分支加上「无第三段才走详情」（`seg[2]` 为空）的判定后修复。
- **推送 GitHub 卡在认证**：本机没装 gh、没有 SSH key、也没存凭据，Git Credential Manager 弹窗等待登录超时。重新发起推送让 GCM 弹浏览器，登录授权后推送成功，凭据被记住。

## 明日计划
- 与后端联调：关闭 mock（`VITE_USE_MOCK=false`）对接真实接口，核对注册返回 token、统计需传 planId 等契约假设。
