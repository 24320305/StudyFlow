# D1 日报

> 2026-09-01（第 1 天）

## 今日目标
- 阅读两份分工文档，明确自己（用户端前端）的职责范围和验收标准。
- 初始化 Vue3 + Vite + TypeScript 前端项目。
- 搭好核心层：统一 Axios 客户端、认证状态、路由守卫、状态常量、契约类型。

## 实际进展
- 完成需求拆解：明确要交付登录/注册、首页、计划、打卡、统计 5 个页面，以及 client / store / router / constants 4 个核心层文件。
- 初始化项目并安装 Element Plus、Pinia、Vue Router、Axios，配置 `@` 别名和中文 locale。
- 封装全项目唯一 Axios 实例（自动带 JWT、解包统一响应、401 统一登出）。
- 建立后端契约类型（User / StudyPlan / CheckIn / Statistics / Page\<T\>）。
- 用 Pinia 实现认证 store + 全局路由守卫；状态枚举中文文案集中到 `constants.ts`。

## 遇到的问题与解决
- **JS 还是 TS**：拿不准。看到分工文档里社区模块文件名是 `src/api/community.ts`，确认全项目用 TypeScript，统一约定。
- **Element Plus 引入方式**：一开始想按需引入，配自动导入插件后容易漏组件，最终改用全量引入，先保证稳定（chunk 体积留给后续再优化）。

## 明日计划
- 完成 7 个用户端页面（登录/注册/首页/计划列表/计划编辑/打卡/统计）。
- 后端还没就绪，搭一套本地 mock 让页面能独立跑起来。
